import { createModel } from "../../common/base/base.model";
import { SettlementScheduleEntity } from "../entity/settlementSchedule.entity";

export const SettlementScheduleModel = createModel(SettlementScheduleEntity);
