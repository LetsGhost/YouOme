import { createModel } from "../../common/base/base.model";
import { DebtLedgerEntryEntity } from "../entity/debtLedgerEntry.entity";

export const DebtLedgerEntryModel = createModel(DebtLedgerEntryEntity);
